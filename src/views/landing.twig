{% extends 'layout.twig' %}

{% set pageTitle = 'Welcome' |trans %}
{% set subTitle = 'Welcome' |trans %}

{% block content %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">{{ 'Language' |trans }}</div>
            <div class="panel-body">
                {{ 'Current language'|trans }}: <strong>{{ 'locale_lang'|trans}}</strong>
            </div>
        </div>
    </div>


{% endblock %}

